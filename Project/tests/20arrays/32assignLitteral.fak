@ = b = new [3] = {0, 1}
b[2][1] = 34
;{ the piece of code to initialize the array is replayed several time.
It;s probably for the best, it allows to initialize arrays with iterators quite idiomatically.think about it after functions
(does it ? )
The alternative (copy of the same value) is really easy to accomplish anyway.
;}
@ =b